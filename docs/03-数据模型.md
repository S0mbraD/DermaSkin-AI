# 数据模型文档

## 1. 核心接口

### CourseConfig — 课程配置

每门皮肤科操作课程对应一个 CourseConfig 对象，系统通过该对象自动渲染学员卡片、评分表、循证依据卡片、雷达图和 AI 建议。

```typescript
interface RubricDim {
  key: string;        // 维度唯一标识
  label: string;      // 中文名称
  maxScore: number;   // 满分分值
}

interface CourseConfig {
  id: string;
  name: string;
  shortName: string;
  videoSrc: string;
  rubric: RubricDim[];
  evals: Record<number, { scores: number[]; timestamps: number[] }>;
  evidenceText: Record<number, { excellent: string; pass: string; fail: string }>;
  getLevel: (score: number, maxScore: number) => 'excellent' | 'pass' | 'fail';
  aiSuggestionGen: (...) => string;
}
```

### Student — 学员

```typescript
interface Student {
  id: number;
  name: string;
  avatar: string;
  grade: string;
  group: string;
  sessionsCount: number;
  attendance: number;
  avgAttention: number;
  avgEngagement: number;
  knowledgeMastery: number;
  practiceHours: number;
  interventions: number;
  recentScores: number[];
  skills: Record<string, number>;
  epaProgress: Record<string, { level: 1|2|3|4; lastAssessed: string }>;
}
```

## 2. 全局常量

```typescript
const LEVEL_META = {
  excellent: { color: '#05CD99', label: '优秀' },
  pass:      { color: '#4361EE', label: '合格' },
  fail:      { color: '#EF4444', label: '不合格' },
};
```

## 3. 课程清单

| ID | 名称 | 短名 | 维度数 |
|----|------|------|--------|
| derma_fungal | 皮肤科真菌镜检操作 | 真菌镜检 | 5 |
| derma_biopsy | 皮肤活检术 | 皮肤活检 | 6 |
| derma_patch | 斑贴试验 | 斑贴试验 | 5 |
| derma_tzanck | Tzanck涂片检查 | Tzanck涂片 | 5 |
| derma_dermoscopy | 皮肤镜检查 | 皮肤镜 | 5 |

## 4. EPA 能力框架 (8项)

| ID | 代码 | 名称 | 分类 | 权重 |
|----|------|------|------|------|
| epa-derm-1 | EPA-1 | 无菌操作与个人防护 | 基础操作 | 1.0 |
| epa-derm-2 | EPA-2 | 皮肤取材与标本处理 | 基础操作 | 1.5 |
| epa-derm-3 | EPA-3 | 真菌镜检制片与判读 | 诊断技术 | 1.5 |
| epa-derm-4 | EPA-4 | 皮肤活检操作 | 诊断技术 | 2.0 |
| epa-derm-5 | EPA-5 | 皮肤镜图像分析 | 诊断技术 | 1.5 |
| epa-derm-6 | EPA-6 | 过敏原检测操作 | 治疗操作 | 1.0 |
| epa-derm-7 | EPA-7 | 患者沟通与知情同意 | 沟通能力 | 1.2 |
| epa-derm-8 | EPA-8 | 临床意义阐述与诊断思维 | 沟通能力 | 1.3 |
